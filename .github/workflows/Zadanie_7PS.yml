name: Test zadania 7

on:
  push:
    branches: [ "Zadanie_7PS" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Test7
      run: python tests_7.py
