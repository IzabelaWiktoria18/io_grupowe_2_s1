name: Python application

on:
  push:
    branches: [ "Zadanie_4JG" ]
  pull_request:
    branches: [ "main" ]  

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Powitanie
      run: echo "Witaj w GitHub Actions!!!!!!"
    - name: Set up Python 3.10
      uses: actions/setup-python@v3
      with:
        python-version: "3.10"